{
  "name": "posta",
  "version": "1.0.0",
  "private": true,
  "description": "Posta - Cloud POS System (Cloudflare Workers + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.5.4"
  }
}
